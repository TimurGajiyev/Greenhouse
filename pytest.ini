# Конфигурация pytest.
# testpaths — собирать тесты ТОЛЬКО из tests/: в reference/ лежат
# чужие репозитории (REopt.jl, Calliope) со своими тест-наборами,
# которые нам запускать не нужно (и нечем — их зависимостей нет).
[pytest]
testpaths = tests
